# AGENT 指南：LiLF95Catch 项目协作约定

本文件适用于当前仓库的整个目录树，后续开发应遵循本说明中的约定。如果有更细粒度的 AGENTS 文件，会覆盖对应子目录中的规则。

---

## 项目目标与范围

- 本仓库用于实现一个针对 F95Zone 论坛（目前主要是 `Lessons in Love` 帖子）的：
  - Python 抓取与数据维护工具（纯 JSON 分片存储 + 本地图片缓存）。
  - 基于 Next.js 的本地查看器（支持过滤、搜索等）。
- 设计和数据结构的权威说明见根目录的 `PLAN.md`。如果实现与 `PLAN.md` 不一致，应优先更新实现并同步修订 `PLAN.md`。

---

## 交互与工作方式

- 默认假设需求已足够明确，可以直接进入实现与迭代。
- 除非出现以下情况，否则在开发过程中**不要频繁向用户提问**：
  - 碰到阻塞性瓶颈（例如关键页面结构与设计完全不符，或权限/反爬限制导致流程无法推进）。
  - 涉及明显破坏性操作（大规模重构、删除数据文件、变更外部接口等）。
  - 需要在多个互斥的设计方向中做出难以回滚的选择。
- 优先策略：
  - 在不牺牲安全性和清晰度的前提下，自主做出合理的工程决策。
  - 尽量做到“能跑起来的最小可交付版本”，然后再向用户汇报结果和待确认事项。

---

## 代码与目录约定

- 整体结构（可随着实现适度演进）：
  - `crawler/`：Python 抓取与 JSON 维护逻辑。
    - `config/`：配置（Cookie、线程 ID、抓取参数等）。
    - `lilf95_crawler/`：Python 包源码。
  - `data/`：抓取产生的 JSON 分片与图片缓存（视为“运行时数据”，不必纳入核心逻辑的代码审查）。
  - `viewer/`：Next.js 前端项目。
  - 根目录的 `PLAN.md`：设计文档，应与实现保持同步。

- 如果新增目录或模块：
  - 与上述结构精神保持一致（抓取逻辑与展示逻辑分离、配置与数据分离）。
  - 不随意把运行时数据混入源码目录（避免混淆）。

---

## Python 代码风格与技术选型

- 版本与风格：
  - 假定使用 Python 3.10+。
  - 强烈建议使用类型注解（type hints），公共函数/方法尽量补全类型。
  - 使用 `pathlib.Path` 处理路径，避免手写字符串拼接路径。
  - 使用 `logging` 标准库记录日志，不用到处 `print`。

- 设计原则：
  - 抓取、解析、存储、用户信息维护、进度管理之间保持清晰分层。
  - 减少全局状态，优先通过显式参数或小型类（例如 `CrawlerContext`）传递依赖。
  - 以 `PLAN.md` 描述的数据结构为主，必要时可以做小改动，但要在文档中反映。

- 命名与可读性：
  - 避免一两个字母的变量名（循环索引除外）。
  - 函数名/变量名使用 `snake_case`，类名使用 `PascalCase`。
  - 不随意添加冗长注释，代码能自解释即可；复杂逻辑在函数/模块 docstring 中说明。

---

## Next.js / 前端代码约定

- 优先考虑使用 TypeScript：
  - 组件与 API Route 使用 `.tsx` / `.ts`。
  - 为核心数据结构定义明确的 TypeScript 接口，与 `PLAN.md` 的 JSON schema 对应。

- 项目风格：
  - 使用函数式组件与 React Hooks，不使用类组件。
  - 前后端职责清晰：数据准备尽量在 API Route 或服务器端完成，前端只做展示与交互。
  - 访问本地 `data/` 时通过 Node 侧的 `fs` 访问，不在浏览器端直接读文件系统路径。

- UI/UX：
  - 优先实用性，简单清晰即可，不强制美术设计。
  - 考虑到帖子数据体量较大，注意列表虚拟化/懒加载等性能细节。

---

## 数据与隐私注意事项

- 不在代码库中硬编码 Cookie 或任何敏感凭据：
  - Cookie/登录状态应通过配置文件或环境变量注入，示例配置中使用占位符。
- 本项目可能处理大量从 F95Zone 抓取的内容：
  - 把 `data/` 视为用户本机数据，不假定这些数据会公开分发。
  - 不在日志或错误信息中输出完整的 Cookie 等敏感信息。

---

## 测试与验证

- 测试策略：
  - 当前仓库尚无测试框架约定时：
    - Python 部分优先使用 `pytest` 风格（如需添加测试）。
    - Next.js 部分可在后期再补充测试，初期以手动打开查看器验证为主。
  - 不为一切小改动强制添加测试，但可为复杂解析逻辑或关键数据转换编写针对性单元测试。

- 验证优先级：
  - 对解析器与存储逻辑，优先使用当前仓库中的本地 HTML（如 `f95_page_3030.html`）进行离线验证。
  - 对网络请求相关逻辑，注意频率和错误处理，避免对远端站点产生不必要负担。

---

## 文档与演进

- 所有重要的结构和流程变更应在以下至少一处体现：
  - `PLAN.md`：高层设计与 schema 演进。
  - 相关模块/类的 docstring。
- 当实现与设计产生差异时：
  - 先以实际需要为准调整实现；
  - 然后同步更新 `PLAN.md`，保持文档对未来开发者可信。

---

如遇到未在本文件覆盖的特殊情况，可以依据通用工程常识做出判断；只有在决策会明显影响整体架构或用户使用方式时，再向用户确认。***
